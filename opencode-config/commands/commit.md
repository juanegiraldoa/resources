Follow the next actions:

- Stage the files provided by the user via `$ARGUMENTS`. If no files are specified, stage all changes (i.e., all modified, new, and deleted files in the working directory).
- Analyze the changes that have been staged and generate a commit message that summarizes them. The message should be written in the same language as the majority of previous commits in the repository.
- Execute the `git commit` command using the generated message.
