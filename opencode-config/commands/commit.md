Follow the next actions:

- Add to stage the files sended by the user $ARGUMENTS if the user don't specify any file do the action on all files
- Depending on the changes that have been made, it creates a commit message
- Executes git commit command
